# 模块开发完成进度（阶段一MVP LLM）

状态：进行中（用于持续更新）
更新时间：2026-03-25（代码开发轮次更新）

## 一、进度总览

1. 总体完成度：97%
2. 阻塞项：0 个
3. 风险项：1 个（上游模型波动导致输出长度不稳定）
4. LLM 协议变更：从 Anthropic 协议切换到 OpenAI 兼容协议（MiniMax M2.5-highspeed）
5. 思维链过滤：`<think>...</think>` 标签在 `llm_client.py` 中自动过滤，前端不可见
4. 远端验证：pytest 15/15 全部通过，Docker 重新部署成功（192.168.2.66）

## 二、模块进度明细

| 模块 | 状态 | 完成度 | 负责人 | 代码文件 | 下一步 |
|---|---|---:|---|---|---|
| Prompt 模板体系 | 已完成 | 100% | Dev-Agent-A | `services/prompts.py` (PROMPT_VERSION=v1.0) | - |
| Prompt 导出对齐 | 已完成 | 100% | Dev-Agent-A | 别名 + build 函数已补齐 | - |
| LLMClient 统一封装 | 已完成 | 100% | Dev-Agent-A | `services/llm_client.py` | 已切换至 OpenAI 兼容协议（API 端点：`https://api.minimaxi.com/v1`，模型：`MiniMax-M2.5-highspeed`，在 `llm_providers.yaml` 中配置） |
| 思维链过滤 | 已完成 | 100% | Dev-Agent-A | `services/llm_client.py` | 在 `call_text` / `call_text_stream` 中自动过滤 `<think>...</think>` 标签（MiniMax M2.5-highspeed 模型输出） |
| IntentService | 已完成 | 100% | Dev-Agent-B | `services/intent_service.py` | 增加低置信度回退策略 |
| QueryService | 已完成 | 100% | Dev-Agent-B | `services/query_service.py` | - |
| AnswerService | 已完成 | 100% | Dev-Agent-C | `services/answer_service.py` | 控制长回答可读性 |
| 合规校验器 | 已完成 | 100% | Dev-Agent-B | `services/compliance.py` | 扩展禁用表达词库 |
| JSON Schema 校验 | 已完成 | 100% | Dev-Agent-B | `services/schema_validator.py` | - |
| Orchestrator Adapter | 已完成 | 100% | Dev-Agent-C | `services/orchestrator_adapter.py` | LangGraph 实现（阶段二） |
| 失败样例回放机制 | 已完成 | 100% | Dev-Agent-E | `services/replay.py` | 建立周度样例复盘流程 |
| LLM 单元测试 | 已完成 | 100% | Dev-Agent-D | `tests/test_llm_services.py` (7用例) | 补充边界场景 |
| 回放框架测试 | 已完成 | 100% | Dev-Agent-E | `tests/test_replay.py` (4用例) | - |

## 三、上线前必达项

1. 合规校验器接入 AnswerService 输出链路（当前独立模块，待集成）。
2. Schema 校验器接入 IntentService/QueryService 的 LLM 返回解析（当前独立模块，待集成）。
3. 持续积累失败样例并定期回放验证 Prompt 优化效果。
4. 验证 OpenAI 兼容协议在所有 LLM 调用场景（意图识别/查询生成/回答生成）下的稳定性。
5. 确认思维链过滤在流式输出中的正确性（跨 chunk 的 `<think>` 标签处理）。

