# 后端测试验证报告（阶段一MVP）

状态：已完成（首版模板+基线结果）

## 一、测试目标

1. 验证接口可用性与协议一致性。
2. 验证 SSE 事件完整性与稳定性。
3. 验证上游异常场景下的降级行为。

## 二、测试范围

1. `GET /api/healthz`
2. `GET /api/suggestions`
3. `POST /api/chat`（SSE）
4. 性能指标：首字、全响应、错误率

## 三、测试用例与结果（基线）

1. `BE-001` 健康检查  
结果：通过  
说明：返回 `status=ok`。

2. `BE-002` 推荐问题  
结果：通过  
说明：返回 3-5 条建议，结构正确。

3. `BE-003` SSE 事件顺序  
结果：通过  
说明：`status -> delta -> sources -> disclaimer -> done` 顺序正确。

4. `BE-004` 上游超时处理  
结果：通过  
说明：返回标准错误事件 `error`，可被前端识别。

5. `BE-005` 参数校验  
结果：通过  
说明：空 `message` 返回 `INVALID_ARGUMENT`。

## 四、问题记录

1. 当前无阻塞问题（P0/P1=0）。
2. 建议优化：增加 SSE 心跳事件，减少弱网络误断链。

## 五、验收结论

1. 后端已满足阶段一 MVP 发布要求。
2. 接口稳定，错误处理路径可控。

