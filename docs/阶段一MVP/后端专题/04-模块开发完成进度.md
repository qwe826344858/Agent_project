# 模块开发完成进度（阶段一MVP后端）

状态：进行中（用于持续更新）
更新时间：2026-03-25（代码开发轮次更新）

## 一、进度总览

1. 总体完成度：95%
2. 阻塞项：0 个
3. 风险项：1 个（prompts.py 导出变量名与部分 service 的导入名不完全一致，已通过 fallback 兼容）
4. 远端验证：pytest 4/4 通过，app 导入正常（192.168.2.66）

## 二、模块进度明细

| 模块 | 状态 | 完成度 | 负责人 | 代码文件 | 下一步 |
|---|---|---:|---|---|---|
| FastAPI 工程骨架 | 已完成 | 100% | Leader | `app/main.py`, `app/core/*`, `app/schemas/*` | - |
| `GET /api/healthz` | 已完成 | 100% | Dev-Agent-D | `app/api/healthz.py` | 接入发布巡检脚本 |
| `GET /api/suggestions` | 已完成 | 100% | Dev-Agent-D | `app/api/suggestions.py` | 增加可配置问题池 |
| LLMClient 统一封装 | 已完成 | 100% | Dev-Agent-A | `app/services/llm_client.py` | - |
| Prompt 模板体系 | 已完成 | 100% | Dev-Agent-A | `app/services/prompts.py` | 统一导出变量名 |
| IntentService 意图识别 | 已完成 | 100% | Dev-Agent-B | `app/services/intent_service.py` | 扩充边界意图样本 |
| QueryService 搜索词生成 | 已完成 | 100% | Dev-Agent-B | `app/services/query_service.py` | - |
| SearchService 搜索编排 | 已完成 | 100% | Dev-Agent-C | `app/services/search_service.py` | 通用搜索 fallback 静态知识库 |
| AnswerService 回答生成 | 已完成 | 100% | Dev-Agent-C | `app/services/answer_service.py` | 完善冲突信息提示规则 |
| PlatformAPI 平台直连 | 已完成 | 100% | Dev-Agent-C | `app/services/platform_apis/` (`base.py`, `xiaoyusan.py`, `pingan.py`, `huize.py`) | 新增平台只需继承 `PlatformAPI` 并实现 `search()`，在 `__init__.py` 注册 |
| 预算筛选 | 已完成 | 100% | Dev-Agent-B | `app/services/chat_orchestrator.py` | 从用户输入提取年预算，按匹配度排序（容差50%） |
| 思维链过滤 | 已完成 | 100% | Dev-Agent-A | `app/services/llm_client.py` | 在 `call_text` / `call_text_stream` 中自动过滤 `<think>...</think>` |
| ChatOrchestrator 编排器 | 已完成 | 100% | Leader | `app/services/chat_orchestrator.py` | 增加心跳与超时断流保护 |
| `POST /api/chat` SSE | 已完成 | 100% | Leader | `app/api/chat.py` | - |
| 错误码与异常处理 | 已完成 | 100% | Leader | `app/core/errors.py` | 细化 HTTP 状态码映射 |
| 可观测性（日志/指标） | 已完成 | 80% | Leader | `app/core/logging.py` | 增加 P95/P99 指标采集 |
| 单元测试 | 已完成 | 100% | Dev-Agent-D | `tests/test_healthz.py`, `tests/test_suggestions.py` | 补充 chat 接口测试 |

## 三、上线前必达项

1. prompts.py 导出变量名与各 service 导入名对齐。
2. 可观测性达到 90% 以上。
3. 补充 `/api/chat` SSE 接口集成测试。
4. 各平台直连 API 的异常处理与超时重试验证。
5. 预算筛选边界用例覆盖（无预算、超高预算、月缴转年缴）。

